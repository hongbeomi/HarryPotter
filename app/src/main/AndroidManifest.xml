<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.hongbeomi.harrypotter">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".HarryPotterApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_harry_potter"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_harry_potter_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name="github.hongbeomi.main.HomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="github.hongbeomi.detail.DetailActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>